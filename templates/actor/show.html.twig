{% extends 'base.html.twig' %}

{% block title %}Actor{% endblock %}

{% block body %}
    <h1 class="text-center">Acteurs</h1>
<div class="container">
    <div class="row">
    <table class="table">
        <tbody>
            <tr>
                <th>#</th>
                <td>{{ actor.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ actor.name }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ actor.picture }}</td>
            </tr>
        </tbody>
    </table>

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
    <a href="{{ path('actor_edit', {'slug': actor.slug}) }}">Editer</a>
    {{ include('actor/_delete_form.html.twig') }}
    {% endif %}
    </div>
</div>
    <a href="{{ path('actor_index') }}">Retour à la liste</a>
{% endblock %}
