{% extends 'base.html.twig' %}

{% block title %}Edit Season{% endblock %}

{% block body %}
    <h1 class="text-center" style="margin: 30px">Editer une saison</h1>

    {{ include('season/_form.html.twig', {'button_label': 'Mettre à Jour'}) }}

    <div class="container h-100" style="margin-top: 10px">
        <div class="row align-items-center justify-content-center h-100">
            <div class="col-12">
                <div>
                    {{ include('season/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>

    <a href="{{ path('season_index') }}">Retour à la liste</a>

{% endblock %}
